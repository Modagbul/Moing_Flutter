import 'package:flutter/material.dart';

class BoardMissionScreen extends StatelessWidget {
  const BoardMissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('BoardMissionPage', style: TextStyle(backgroundColor: Colors.white),),
    );
  }
}
